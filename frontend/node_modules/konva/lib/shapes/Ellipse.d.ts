import { Shape, ShapeConfig } from 'konva/lib/Shape.js';
import { Context } from 'konva/lib/Context.js';
import { GetSet, Vector2d } from 'konva/lib/types.js';
export interface EllipseConfig extends ShapeConfig {
    radiusX: number;
    radiusY: number;
}
export declare class Ellipse extends Shape<EllipseConfig> {
    _sceneFunc(context: Context): void;
    getWidth(): number;
    getHeight(): number;
    setWidth(width: number): void;
    setHeight(height: number): void;
    radius: GetSet<Vector2d, this>;
    radiusX: GetSet<number, this>;
    radiusY: GetSet<number, this>;
}
